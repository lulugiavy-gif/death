local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer

local Window = Rayfield:CreateWindow({
    Name = "⚡ SCRIPT CANH SÉT | VIP",
    KeySystem = true,
    KeySettings = {
        Title = "VIP KEY SYSTEM",
        Subtitle = "Bảo mật",
        FileName = "MyScriptConfig",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"deathvip"} -- Key bảo mật của bạn
    }
})

local Tab = Window:CreateTab("Canh Sét", 4483363465)
local isRunning = false
local lastPlantTime = 0

Tab:CreateToggle({
    Name = "Đoán sét chuẩn 3 nhịp",
    CurrentValue = false,
    Callback = function(v)
        isRunning = v
        if v then
            Rayfield:Notify({Title = "ĐÃ BẬT", Content = "Hãy trồng cây để bắt đầu...", Duration = 1.5})
            
            local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            
            Workspace.ChildAdded:Connect(function(child)
                if not isRunning then return end
                
                if char and char:FindFirstChild("HumanoidRootPart") then
                    local hrp = char.HumanoidRootPart
                    if child:IsA("Model") or child:IsA("Part") then
                        local pos = (child:IsA("Model") and child.PrimaryPart and child.PrimaryPart.Position) or (child:IsA("Part") and child.Position)
                        
                        if pos and (pos - hrp.Position).Magnitude < 12 then
                            local n = string.lower(child.Name)
                            if string.find(n, "plant") or string.find(n, "seed") or string.find(n, "tree") or string.find(n, "crop") or string.find(n, "stem") then
                                
                                local currentTime = tick()
                                if currentTime - lastPlantTime >= 1.0 then
                                    lastPlantTime = currentTime
                                    
                                    task.spawn(function()
                                        -- Nhịp 1
                                        task.wait(1.5) 
                                        if not isRunning then return end
                                        Rayfield:Notify({Title = "Đếm nhịp", Content = "Nhịp 1/3", Duration = 0.5})
                                        
                                        -- Nhịp 2
                                        task.wait(1.5) 
                                        if not isRunning then return end
                                        Rayfield:Notify({Title = "Đếm nhịp", Content = "Nhịp 2/3", Duration = 0.5})
                                        
                                        -- Nhịp 3 (Thu hoạch)
                                        task.wait(1.5) 
                                        if not isRunning then return end
                                        Rayfield:Notify({Title = "⚡ THU HOẠCH NGAY!", Content = "Nhịp 3/3 - Bấm ngay!", Duration = 2.0})
                                    end)
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})

Tab:CreateButton({
    Name = "🔄 Đổi Server Vắng (Hop 1 người)",
    Callback = function()
        Rayfield:Notify({Title = "SERVER HOP", Content = "Đang tìm phòng vắng...", Duration = 1.5})
        local servers = {}
        local req = game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")
        local body = HttpService:JSONDecode(req)
        
        if body and body.data then
            for _, s in pairs(body.data) do
                if type(s) == "table" and s.playing and s.maxPlayers and s.id then
                    -- Tìm server có ít người nhất
                    if s.playing < 2 then 
                        table.insert(servers, s.id)
                    end
                end
            end
        end
        
        if #servers > 0 then
            local targetServer = servers[math.random(1, #servers)]
            TeleportService:TeleportToPlaceInstance(game.PlaceId, targetServer, Players.LocalPlayer)
        else
            Rayfield:Notify({Title = "SERVER HOP", Content = "Không tìm thấy server vắng!", Duration = 2})
        end
    end,
})
                                        task.wait(2.5) -- Thời gian đến nhịp 3 (Báo trước 2s để thu hoạch)
                                        if not isRunning then return end
                                        Rayfield:Notify({
                                            Title = "⚡ THU HOẠCH NGAY!", 
                                            Content = "Trước sét 2 giây!", 
                                            Duration = 2
                                        })
                                    end)
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})

Tab:CreateButton({
    Name = "🔄 Đổi Server Vắng Người",
    Callback = function()
        Rayfield:Notify({Title = "SERVER HOP", Content = "Đang tìm phòng...", Duration = 1.5})
        local servers = {}
        local req = game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")
        local body = HttpService:JSONDecode(req)
        
        if body and body.data then
            for _, s in pairs(body.data) do
                if type(s) == "table" and s.playing and s.maxPlayers and s.id then
                    if s.playing < s.maxPlayers - 1 then
                        table.insert(servers, s.id)
                    end
                end
            end
        end
        
        if #servers > 0 then
            local targetServer = servers[math.random(1, #servers)]
            TeleportService:TeleportToPlaceInstance(game.PlaceId, targetServer, Players.LocalPlayer)
        else
            Rayfield:Notify({Title = "SERVER HOP", Content = "Không tìm thấy server!", Duration = 2})
        end
    end,
})
